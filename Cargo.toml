[package]
name = "search-api"
version = "0.1.0"
edition = "2021"
authors = ["Marketly Team <eng@marketly.com>"]
description = "Product search service for the Marketly e-commerce platform"
license = "MIT"

[[bin]]
name = "search-api"
path = "src/main.rs"

[dependencies]
axum = { version = "0.7", features = ["macros"] }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tower = "0.5"
tower-http = { version = "0.5", features = ["trace", "cors"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
once_cell = "1"
thiserror = "1"

[dev-dependencies]
axum-test = "14"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
